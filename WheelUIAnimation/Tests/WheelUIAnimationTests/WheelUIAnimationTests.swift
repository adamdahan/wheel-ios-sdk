import XCTest
@testable import WheelUIAnimation

final class WheelUIAnimationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WheelUIAnimation().text, "Hello, World!")
    }
}
